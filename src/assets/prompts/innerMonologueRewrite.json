{
    "name": "rewrite as inner mologue",
    "trigger": "selection",
    "mode": "replace",
    "actions": [
      {
        "type": "generate",
        "template": "Rewrite the viewpoint of the following text as an inner monologue of the main character. The paragraph should be no more than one sentence long. You write with a humorous, yet critical tone. Input text: ::selection::"
      }
    ]
  }